name: Main pipeline

on:
  push:
    branches: [ "master", "develop", "main" ]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: checkout repo
        uses: actions/checkout@v3
      - name: run ruff
        uses: # TODO: insert ruff here
  test:
    runs-on: ubuntu-latest
    steps:
      - name: checkout repo
        uses: actions/checkout@v3
      - name: run pytest
        run: # todo: run pytest
  build:
    runs-on: ubuntu-latest
    steps:
      - name: checkout repo
        uses: actions/checkout@v3
      - name: build image
        run: # todo: build dockerfile to check it works
